Edit a power:
<form action="/powers/<%=@power.id%>" method="post" >
  <input id="hidden" type="hidden" name="_method" value="patch">
  Power name: <input type="text" name="power[name]" value="<%=@power.name%>" /><br>
  Pick the aliens who have this power: <br>
  <%Alien.all.each do |alien| %>
    <input type="checkbox" name="power[alien_ids][]" id="<%=alien.id%>" value="<%=alien.id%>" <%='checked' if @power.alien_ids.include?(alien.id) %> ><%=alien.name%></input>
    <br>
  <%end%>
  <input type="submit" value="submit" />
</form>
